# Configuration Directory

This directory contains application configuration files.

## Files

- `nginx.conf` - Nginx configuration for frontend server

## Usage

Configuration files are typically used by deployment scripts or Docker containers.

For application configuration (paths, settings), see `src/ledger/config/`.

